// Define all your script links
const scriptLinks = [
    'https://www.gstatic.com/firebasejs/8.6.1/firebase-app.js',
    'https://www.gstatic.com/firebasejs/8.6.1/firebase-database.js',
    'login.js',
    'generate.js'
];

// Loop through the script links and dynamically create script tags
scriptLinks.forEach(link => {
    /*
    const script = document.createElement('script');
    script.src = link;
    document.head.appendChild(script);

     */
});




